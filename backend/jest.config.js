/**
 * configuration for Jest Unit testing
 * @constant
 */
const JestConfig = {
    testEnvironment: 'node',
    verbose: true,
    bail: true,
    testTimeout: 10000
};

module.exports = JestConfig;